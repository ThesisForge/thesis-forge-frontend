import { Thesis } from "@/lib/types";

export const fetchUserThesis = async (token: string): Promise<Thesis[]> => {
//   const resp = await fetch("backendURl", {
//     headers: {
//       Authorization: `Bearer ${token}`,
//     },
//   });
//   const data: Thesis[] = await resp.json();
//   return data;
    const data: Thesis[] = []
    return data
};
